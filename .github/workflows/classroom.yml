name: Autograding Tests
'on':
- push
- repository_dispatch
permissions:
  checks: write
  actions: read
  contents: read
jobs:
  run-autograding-tests:
    runs-on: ubuntu-latest
    if: github.actor != 'github-classroom[bot]'
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Run Coffee Shop Tests
      id: run-coffee-shop-tests
      uses: classroom-resources/autograding-command-grader@v1
      with:
        test-name: Run Coffee Shop Tests
        setup-command: npm install
        command: npm run test
        timeout: 5
        max-score: 100
    - name: Autograding Reporter
      uses: classroom-resources/autograding-grading-reporter@v1
      env:
        RUN-COFFEE-SHOP-TESTS_RESULTS: "${{steps.run-coffee-shop-tests.outputs.result}}"
      with:
        runners: run-coffee-shop-tests
